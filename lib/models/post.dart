class Post {
  String pid;
  String title;
  String description;
  String uid;
  String imageName;

  Post(pid, title, description, uid, imageName) {
    this.pid = pid;
    this.title = title;
    this.description = description;
    this.uid = uid;
    this.imageName = imageName;

  }
}